generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// TODO: Definir modelos cuando se tenga el contexto completo del sistema
// Este schema se reemplazará por completo en la siguiente etapa
